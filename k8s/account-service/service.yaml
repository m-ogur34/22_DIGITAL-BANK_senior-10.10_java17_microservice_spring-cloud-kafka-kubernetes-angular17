apiVersion: v1
kind: Service
metadata:
  name: account-service
  namespace: digitalbank
spec:
  selector:
    app: account-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP
